import React from 'react'

const EditUser = (props) => {
    console.log(props.match.params.id)
    console.log(props.match.params.id)
    return (
      
        <div>
        {console.log(props.match.params.id)}
        </div>
    )
}

export default EditUser
